# 🚀 公司内部标准：AI 辅助开发与代码审查指南 (SOP)

**制定目的：**
本指南旨在规范开发团队如何高效、安全地使用 AI 辅助工具（如 Cursor、Claude、Gemini 等）进行软件开发。所有项目必须严格遵守“先设计、后生成、再审查”的原则，以确保代码质量、系统安全性及架构的可维护性。

---

## 第一阶段：基础设施与技术选型 (Infrastructure & Architecture)
**⚠️ 核心原则：禁止在未确认架构前直接让 AI 生成完整系统代码。**

1. **确定项目安全级别：**
   在项目启动前，必须界定项目属于 **Level 1 (内部普通)**、**Level 2 (公开/内部高权限)** 还是 **Level 3 (高风险/涉及资金)**（详见第五阶段）。

2. **决定架构模式：**
   明确项目使用的架构设计（例如 MVC 模式、前后端分离 SPA 或 微服务架构）。
   

3. **统一技术栈 (Tech Stack) 规范：**
   - **后端框架 (Backend)：** 必须在 **Node.js / Python / PHP** 之间根据项目特性（如 I/O 密集、数据分析、快速建站）进行选择。
   - **前端框架 (Frontend)：** 不强制绑定单一框架，允许使用主流技术（如 React, Vue, Next.js 等）。
     - **🚨 强制要求：** 项目负责人必须在 `README.md` 或项目工单中，**写下不少于 50 字的前端框架选择原因**（例如业务需求、组件库生态等），避免盲目追逐新技术。
   - **数据库 (Database)：**
     - **关系型数据库 (RDBMS)：** 凡涉及订单、用户权限、资金交易，**必须**使用 PostgreSQL 或 MySQL。
     - **非关系型数据库 (NoSQL)：** 仅在处理日志 (Logs)、非结构化大数据时，允许使用 MongoDB。
     - *(注：如需偏离上述标准技术栈，必须事先取得技术总监/Tech Lead 批准。)*

---

## 第二阶段：前端与界面生成 (UI/UX Generation)
**目标：利用 AI 快速构建视觉原型，与业务方对齐需求。**

1. **内部系统 (Internal System) - 无 Figma 设计图：**
   - **流程：** 允许开发者使用文字描述，结合 Claude Sonnet 及 Tailwind CSS/Bootstrap 快速生成 UI 草图。
   - **原则：** 追求“功能优先、排版整洁”，无需过度雕琢视觉。必须与内部需求方确认 User Flow (用户流程) 后，方可进入后端开发。

2. **公开项目 (Public System) - 有专属 Figma 设计图：**
   - **流程：** 严禁开发者或 AI“自由发挥”。必须严格依照 UI/UX 团队提供的 Figma 设计图进行开发。
   - **操作：** 可将 Figma 截图交由 AI 分析并生成基础 CSS 与组件结构，但最终视觉效果必须与设计图 100% 对齐。

---

## 第三阶段：AI 工具库使用规范 (When to use what?)
为将开发效率最大化，请根据当前任务性质，切换使用以下三大 AI 引擎：

1. **Cursor Auto (内置 Composer / Cmd+K / Cursor Tab)**
   - **定位：** 你的“无情打字机”与“肌肉记忆”。
   - **适用场景：** 编写单一函数、添加 Error Handling、代码自动补全、生成样板代码 (Boilerplate) 及终端报错时的快速原地修复。

2. **Claude Sonnet (例如 Sonnet 4.6 或最新版)**
   - **定位：** 你的“高级前端/重构专家”。
   - **适用场景：** 根据截图从零生成页面组件、大规模的文件结构重构 (File Restructure)、编写格式优美且可读性高的标准业务逻辑代码。

3. **Gemini 3 Pro**
   - **定位：** 你的“深思熟虑的架构师”与“逻辑除虫大师”。
   - **适用场景：** - 处理超长上下文 (Long Context)，如读取整本第三方 API 文档或海量系统日志。
     - 深层逻辑推理：排查复杂 Bug（如并发竞争 Race Condition、内存泄漏）。
     - 架构与安全审查：作为 Layer 3 防线，审查代码中是否存在潜在的安全漏洞或越权访问。

---

## 第四阶段：数据库与接口设计 (DB & API Design)

1. **数据库设计：** UI 确认后，开发者必须先规划实体关系图 (ER Diagram)。可使用描述性提示词让 AI 生成 SQL Schema 或 ORM Models（例如 Prisma Schema）。
2. **定义 API 契约：** 在编写核心逻辑前，先定义好请求与响应的 JSON 数据结构规范。

---

## 第五阶段：安全分级与代码审查 (Security & Code Review)
所有提交的代码 (Pull Request) 必须经过 CI/CD 流水线验证。请严格根据项目风险级别执行审查：

- 🛡️ **Level 1 (低风险 - 内部普通工具)：**
  - 不涉及敏感隐私或核心数据修改。
  - **要求：** 通过 CI/CD 自动化代码格式检查 (Prettier/ESLint) 及静态安全扫描 (CodeQL)。通过后可由作者自行 Merge。
- 🛡️ **Level 2 (中风险 - 公开资讯 / 内部高权限工具)：**
  - 涉及读取/导出敏感数据，或拥有数据库写入权限。
  - **要求：** 必须通过自动化检查，**并且**要求开发者使用 AI (Gemini/Claude) 进行边界条件辅助自查。涉及核心权限的 PR 需由另一位同事进行 Peer Review。
- 🛡️ **Level 3 (高风险 - 涉及资金/极敏感数据)：**
  - 面向公众且包含支付网关、信用卡资料或核心隐私。
  - **要求：** 在自动化与 AI 审查的基础上，**强制要求**所有涉及交易、扣费、并发处理的代码，必须由 Tech Lead 亲自进行人工 Code Review 并 Approve 后方可 Merge。

---

## 附录：Pull Request (PR) 提交模板
请在每次提交 PR 时，复制以下内容到描述中并如实勾选：


### 📝 PR 检查清单 (PR Checklist)

**1. 项目基础信息：**
- [ ] 本项目安全级别为： `[请填写: Level 1 / 2 / 3]`
- [ ] (仅限前端新建项目) 我已在 README.md 中说明了前端框架的选择原因 (不少于 50 字)。

**2. AI 辅助说明：**
- [ ] 本次代码主要使用了以下 AI 工具进行辅助编写：
  - [ ] Cursor Auto
  - [ ] Claude Sonnet
  - [ ] Gemini 3 Pro
  - [ ] 无，纯手工编写

**3. 安全与质量自查：**
- [ ] 代码已通过本地 ESLint / Prettier 格式化检查。
- [ ] GitHub Actions (CI/CD) 及 CodeQL 安全扫描已全部通过 (显示绿勾)。
- [ ] 我已检查过新增代码的异常处理 (Try/Catch) 及边界条件。
- [ ] (针对 Level 2/3) 核心逻辑变更已邀请相关同事或 Tech Lead 进行 Code Review。

**简要描述本次 PR 的主要改动：**
>
